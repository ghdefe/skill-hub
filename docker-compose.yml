version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: skills_sharing
      POSTGRES_USER: ${DATABASE_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-postgres}
    restart: unless-stopped
    ports:
      - "42150:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  backend:
    image: ghcr.io/ghdefe/skillhub/backend:latest
    environment:
      DATABASE_URL: jdbc:postgresql://postgres:5432/skills_sharing
      DATABASE_USERNAME: ${DATABASE_USERNAME:-postgres}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD:-postgres}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      JWT_SECRET: ${JWT_SECRET}
      GITHUB_WEBHOOK_SECRET: ${GITHUB_WEBHOOK_SECRET}
      ENCRYPTION_SECRET_KEY: ${ENCRYPTION_SECRET_KEY}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
      BACKEND_URL: ${BACKEND_URL:-http://localhost:42151}
    restart: unless-stopped
    ports:
      - "42151:18123"
    depends_on:
      - postgres

  frontend:
    image: ghcr.io/ghdefe/skillhub/frontend:latest
    restart: unless-stopped
    ports:
      - "42152:8080"
    depends_on:
      - backend

volumes:
  pgdata:
